/**
 * Esqueleto. A montagem de verdade — login, provedor da noite e as cinco telas
 * — entra na Task 8, quando `NoiteProvider` e `useOperador` existirem.
 */
export default function App() {
  return <p className="p-8 text-center">Caixa Vivo</p>
}
